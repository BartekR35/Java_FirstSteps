import java.util.Random;

public class HighestLowestNumber {
    public static void main(String[] args) {
        Random random = new Random();



        /*
        // Initialize variables to store the minimum and maximum values
        int min = Integer.MAX_VALUE;
        int max = Integer.MIN_VALUE;

        // Counter for the while loop
        int count = 0;

        // While loop to generate 10 random numbers and find min and max
        while (count < 10) {
            int number = random.nextInt(100) + 1;  // Random number between 1 and 100
            System.out.println("Generated number: " + number);

            // Update min and max if needed
            if (number < min) {
                min = number;
            }
            if (number > max) {
                max = number;
            }

            count++;
        }

        // Display the results
        System.out.println("Minimum number: " + min);
        System.out.println("Maximum number: " + max);
         */
    }
}
